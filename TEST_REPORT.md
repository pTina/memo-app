# 테스트 리포트 — memo-app

- 실행 방법: `python3 -m http.server 8080` (AGENTS.md 안내대로)
- 테스트 방법: Playwright(Chromium, headless)로 실제 브라우저를 띄워 주요 기능을 순서대로 조작하며 콘솔 에러·페이지 에러·네트워크 실패(4xx/5xx)를 수집. 별도 자동화 테스트 스위트(unit/e2e 코드)가 저장소에 없어(빌드 도구 없는 순수 정적 앱), 실제 사용자 흐름을 구동하는 방식으로 검증함.
- 테스트한 흐름: 메모 목록 진입 → 새 메모 생성 → 제목/본문 입력 → 텍스트 선택 시 플로팅 툴바 → 형광펜 적용 → 링크 삽입(클릭 시 새 탭으로 열리는지 포함) → 폰트 관리 모달 → 이미지로 저장(선택 영역 내보내기, PNG 다운로드) → 목록으로 돌아가기 → 검색 → 사용법 모달 → 다크/라이트 모드 전환 → 백업 모달 → 전체 선택 → 일괄 삭제(삭제 확인 모달 포함)
- 날짜: 2026-07-30

## 결과 요약

| 항목 | 결과 |
|---|---|
| 콘솔 에러(console.error) | 0건 |
| 페이지 에러(uncaught exception) | 0건 |
| 실패한 네트워크 요청(4xx/5xx) | 0건 |
| 조작한 UI 흐름 17단계 | 전부 정상 동작 |

CDN 스크립트(jQuery, html2canvas)와 IndexedDB 저장/조회, 폰트 업로드 모달, PNG 내보내기(다운로드까지), 다크모드 전환, 백업 모달, 삭제 확인 플로우 모두 에러 없이 동작을 확인했습니다.

## 발견된 오류

### 1. 메모 목록 미리보기에서 줄바꿈 없이 텍스트가 붙어서 표시됨 (재현됨)

- **위치**: [js/app.js:21](js/app.js#L21) `plainText()` 함수, [js/app.js:124](js/app.js#L124)에서 목록 미리보기 렌더링 시 사용
- **증상**: 본문에 줄바꿈(엔터)이 있는 메모를 목록에서 보면, 줄바꿈이 사라지면서 앞줄의 마지막 글자와 다음 줄의 첫 글자가 공백 없이 붙어서 보임.
  - 재현 예시(직접 생성한 테스트 메모): 편집 화면에는 "Hello world." 다음 줄에 "두 번째 줄입니다."로 정상 표시되지만, 목록 미리보기에는 `Hello world.두 번째 줄입니다.`로 붙어서 표시됨.
  - 저장소에 이미 있던 `화면오류/화면오류1.png`(사용자가 사전에 캡처한 스크린샷, 07.29)에서도 동일 현상 확인: 편집 화면 원문은 `...좋아하거든요...` 다음 줄에 `회사에서는...`으로 이어지지만(`화면오류2.png`, `화면오류3.png`), 목록에서는 `...좋아하거든요..?회사에서는...`처럼 물음표+줄바꿈 없이 바로 붙어버림.
- **원인**: `plainText(html)`이 jQuery의 `$("<div>").html(html).text()`로 HTML을 텍스트로 변환하는데, contenteditable에서 줄바꿈은 보통 `<div>`로 분리된 블록 요소로 저장됨. `.text()`는 블록 요소 사이에 어떤 구분자도 넣지 않고 텍스트 노드만 이어 붙이기 때문에, 줄이 바뀌는 지점에서 공백/개행 없이 병합됨.
- **영향도**: 낮음 — 실제 저장된 메모 데이터(본문)는 손상되지 않고 편집 화면에서는 정상 표시됨. 목록 화면의 미리보기(스니펫) 텍스트에만 나타나는 표시상의 문제.
- **제안**: `plainText()`에서 `.text()` 호출 전에 블록 요소 종료 태그(`</div>`, `</p>`)와 `<br>`를 개행이나 공백으로 치환한 뒤 텍스트를 추출하도록 수정.

## 특이사항 (버그 아님)

- 텍스트를 선택한 상태로 "이미지로 저장"을 열면 메모 전체가 아니라 선택한 부분만 내보내는 미리보기가 뜸 — README에 문서화된 의도된 동작(선택 영역만 내보내기).
- 형광펜·링크 스타일은 이미지 내보내기 미리보기와 실제 편집 화면 모두에서 정확히 유지되는 것을 확인함.

## 참고

발견된 문제(#1)는 이후 `CHANGELOG.md`에 기록된 작업에는 포함되지 않았고, 아직 미해결 상태입니다.
